APP_ENV=development
BASE_URL=http://localhost:8000
SECRET_KEY=change-me-in-production-use-long-random-string

# SQLite (výchozí — pro dev a jednoduché produkční nasazení)
DATABASE_URL=sqlite:///./data/inventory.db

# MariaDB (produkce) — aktivovat profilem: docker-compose --profile mariadb up
# DATABASE_URL=mysql+pymysql://assettrack:assetpass@mariadb/inventory
# DB_ROOT_PASSWORD=rootpass
# DB_NAME=inventory
# DB_USER=assettrack
# DB_PASSWORD=assetpass

FIRST_ADMIN_USER=admin
FIRST_ADMIN_PASS=admin123
